<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.paladin.common.mapper.cache.SysVisitCacheMapper">
	
	<select id="getCache" resultType="com.paladin.common.model.cache.SysVisitCache">
		SELECT * FROM sys_visit_cache WHERE code = #{key} AND ip = #{ip}		
	</select>
	
</mapper>